## Paul Bezilla

![Five repositories: a stack of three, ordered as in the three-repos diagram in kubernetes-platform-reference — the application on top, the platform in the middle, the cloud underneath. otel-service-reference sits at the top; a solid arrow runs down from it to kubernetes-platform-reference, a real but optional edge — the platform deploys this workload and builds its sample image from a sibling checkout at ../otel-service-reference when one is on disk, and a fallback workload when there is none. It does not clone the repository. A dashed arrow runs down from kubernetes-platform-reference to terragrunt-reference-architecture, dashed because that edge is conceptual rather than real: neither repository references the other in code, configuration or CI, and the platform brings itself up on kind. Beside the stack, joined to nothing, stand capsize, a read-only Kubernetes CLI that points at any cluster, and switchyard, an inference gateway that routes across three simulated providers and fails over between them. Neither depends on any of the other three.](docs/stack.svg)

Building these systems exposed failures their checks missed. Five investigations into what went wrong, how I found it, and what changed. Here are **[Five checks that passed while examining nothing](https://gist.github.com/bezilla/dff98698cf41129f5d262eb727ef445a)**.
